
def square(e):
	# edited while pulling the code to the local machine.
	return(e**2)
# Added this comment when present on branch-1.
# This comment should not be visible on other branches.

print(square(5))
